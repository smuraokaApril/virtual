package util;

import java.util.Iterator;
import java.util.List;

public class Decimal {

	/**
	 * ‚Qi”‚ğ‚P‚Oi”‚É•ÏŠ·
	 * 
	 * @param binary ‚Qi”
	 * @return ‚P‚Oi”
	 */
	private String BinaryNumberToDecimalNumber(String binary) {

		ListUtil listUtil = new ListUtil();

		List<Character> list;
		list = listUtil.toList(binary.toCharArray());

		Iterator<Character> iterator;
		iterator = list.iterator();

		iterator.

		while (iterator.hasP()) {
			Character character = (Character) iterator.next();
			
		}

		return null;
	}
}
